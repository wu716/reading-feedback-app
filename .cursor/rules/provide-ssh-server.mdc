---
description: 本项目代码改完要上服务器时，必须提供 SSH 登录命令
alwaysApply: true
---

# 代码改动后自动部署服务器

在 `reading-feedback-app` 里，只要本轮有代码修改且用户没有明确说“先不上线”，按仓库根目录 `AGENTS.md` 和 `.cursor/skills/provide-ssh-server/SKILL.md` 自动更新生产服务器。部署后检查健康状态，并在回复中报告结果。

```bash
ssh ubuntu@43.161.238.165
```

登录后执行 `sudo -i`，再执行：

```bash
cd /opt/shuran-app && git pull origin main && cd deploy/aliyun && docker compose up -d --build
```
