---
description: 本机可打 APK；用户确认已传到服务器后必须删除本机所有 *.apk
alwaysApply: true
---

# 本机 APK：可打、确认上传后必须删

用户要求更新手机外壳时，**允许**在这台 Windows 电脑上用项目现有 Android 构建（`mobile/android` gradle，keystore 已在磁盘）打签名包，输出到 `D:\projects\reading-feedback-app\releases\shuran.apk`。

不要 `git add` apk，不要提交 `*.apk` / `releases/*.apk`。

## 上传后删除（必须）

用户确认文件已在服务器上（例如说「传好了」「上传完了」）之后，**必须删除本机所有 `*.apk`**，包括 `releases/shuran.apk` 以及 `mobile/android` 构建产物目录里的 apk。不要让安装包一直留在这台电脑上。本轮打完、用户尚未确认上传时，不要提前删除。

## 与其他技能的关系

`do-it-and-give-steps`、`auto-push-github` 等技能若写「打 APK」或把安装包放到 `releases/shuran.apk`：按本规则执行——用户要更新手机外壳时本机打包；确认上服务器后删干净。不要把 apk 加入 git。
